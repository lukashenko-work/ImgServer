python -m venv .venv
Set-ExecutionPolicy Unrestricted -Force
.\.venv\Scripts\Activate.ps1
pip install pillow
pip install psycopg2 или psycopg2-binary для Dockerfile и compose.yaml
pip install flask
pip install flask_cors
pip freeze > requirements.txt
python main_file_name.py

echo "# ImgServer" >> README.md
git init
git add .
git commit - m  'feat: first commit'
git branch -M main
git remote add origin https://github.com/lukashenko-work/ImgServer.git
git push -u origin main

#Удалить файл из репозитория
Добавьте файл в .gitignore
Удаляем файл только из репозитория, и физически файл сохранится на диске
git rm --cached --ignore-unmatch logs\app.log
git commit -am "log removing"
git push origin {branch name}

Выкачать проект из репозитория без создания папки проекта
git clone https://github.com/lukashenko-work/ImgHosting.git .

Сброс счетчика id
Для сброса счетчика (последовательности) id в PostgreSQL, если таблица пуста или данные не важны, используйте
TRUNCATE TABLE table_name RESTART IDENTITY;.
Если данные нужно сохранить, используйте
SELECT setval(pg_get_serial_sequence('table_name', 'id'), 1, false); для установки счетчика на 1. 

Логирование запросов в Postgres
ALTER SYSTEM SET log_statement = 'all';
SELECT pg_reload_conf();

имя

yaml

version: '3.8'
services:
  db:
    image: postgres
    command: ["postgres", "-c", "log_statement=all"]
    environment:
      POSTGRES_PASSWORD: example
 
 или

 docker run --name some-postgres -e POSTGRES_PASSWORD=mysecretpassword -d postgres -c log_statement=all

Вместо all используйте mod (логирование изменений данных: INSERT, UPDATE, DELETE) или ddl для уменьшения шума.
Для точного времени запросов добавьте -c log_min_duration_statement=0 (логировать запросы дольше
мс). 

выполнение sql консоли
docker exec -it my_pg_test psql db_test -U postgres
или
docker exec -it my_pg_test psql -h 127.0.0.1 -U postgres -d my_db

запуск командной строки
docker exec -it <имя_или_id_контейнера> /bin/bash

собираем образ приложения
docker build -t img_srv_python_app .

собираем контейнер
docker-compose up -d --build


nginx про ускорение
https://habr.com/ru/articles/198982/

docker run --rm -it busybox

docker run -d --name db --network my-net postgres